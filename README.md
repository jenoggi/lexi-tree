Lexicographic tree, a tree that describes a dictionary in a single-linked tree. Can be used to e.g. generate word suggestions as you get in modern editors while typing.
I used boxes for the implementation, and the rust reserved words as example dictionary (I know, it should be macro_rules not macro-rules, but "-" seemed to make more sense in this application).
